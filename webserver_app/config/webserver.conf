# Generic Platform WebServer Configuration
# Set the target platform (am335x or am62d)
PLATFORM=am62d

# Platform directory (relative to webserver common directory)
PLATFORM_DIR=

# Application directory (where static files are served from)
APP_DIR=

# Server port (default: 3000)
PORT=3000

# Debug logging
DEBUG=false